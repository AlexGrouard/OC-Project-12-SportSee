import { BrowserRouter as Router, Route, Routes } from "react-router-dom"
import styles from "./Layout.module.scss"
import Header from "./Header/Header"
import Sidebar from "./Sidebar/Sidebar"

function Layout(params: type) {
	return (
	)
}
